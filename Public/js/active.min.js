
$(document).ready(function () {
    //高亮当前选中的导航
    var myNav = $(".nav-sidebar > li > a");
    for (var i = 0; i < myNav.length; i++) {
        var links = myNav.eq(i).attr("href");
        var myURL = document.URL;
        var durl=/http:\/\/([^\/]+)\//i;
        domain = myURL.match(durl);
        var result = myURL.replace("http://"+domain[1],"");
        if (links == result) {
            myNav.eq(i).parents("li").addClass("active");
        }
    }
});
$(document).ready(function () {
    //高亮当前选中的导航
    var myNav = $("ul.nav-tabs >li > a");
    for (var i = 0; i < myNav.length; i++) {
        var links = myNav.eq(i).attr("href");
        var myURL = document.URL;
        var durl=/http:\/\/([^\/]+)\//i;
        domain = myURL.match(durl);
        var result = myURL.replace("http://"+domain[1],"");
        if (links == result) {
            myNav.eq(i).parents("li").addClass("active");
        }
    }
});
